<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.catalogmovie">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CatalogMovieApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CatalogMovie">
        <activity
            android:name=".feature.detial_content.ui.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".feature.catalog_movie.ui.ListMovie"
            android:exported="false" />
        <activity
            android:name=".feature.catalog_tv.ui.ListTv"
            android:exported="false" />
        <activity
            android:name=".feature.main.ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".feature.intro.ui.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>